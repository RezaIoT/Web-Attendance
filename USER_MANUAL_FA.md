# راهنمای کاربری سیستم حضور و غیاب دانشجویان

## فهرست مطالب
1. [معرفی سیستم](#معرفی-سیستم)
2. [نیازمندی‌ها](#نیازمندیها)
3. [نصب و راه‌اندازی](#نصب-و-راهاندازی)
4. [ورود به سیستم](#ورود-به-سیستم)
5. [داشبورد اصلی](#داشبورد-اصلی)
6. [مدیریت ماژول‌ها (دروس)](#مدیریت-ماژولها-دروس)
7. [مدیریت دانشجویان](#مدیریت-دانشجویان)
8. [مدیریت جلسات](#مدیریت-جلسات)
9. [ثبت حضور توسط دانشجو](#ثبت-حضور-توسط-دانشجو)
10. [گزارش‌ها و تحلیل‌ها](#گزارشها-و-تحلیلها)
11. [تغییر رمز عبور](#تغییر-رمز-عبور)
12. [تغییر زبان](#تغییر-زبان)

---

## معرفی سیستم

سیستم حضور و غیاب دانشجویان یک برنامه تحت وب است که به اساتید امکان می‌دهد حضور و غیاب دانشجویان را به صورت الکترونیکی ثبت و مدیریت کنند. دانشجویان می‌توانند با استفاده از کلید عبور (Passkey) که استاد در اختیار آنها قرار می‌دهد، حضور خود را ثبت کنند.

### ویژگی‌های اصلی:
- ✅ مدیریت ماژول‌ها (دروس) با کد و ترم
- ✅ افزودن دانشجویان به صورت تکی یا گروهی
- ✅ ایجاد جلسات با کلید عبور اختصاصی
- ✅ ثبت حضور آنلاین توسط دانشجویان
- ✅ گزارش‌های تحلیلی با نمودار
- ✅ پشتیبانی از زبان فارسی و انگلیسی
- ✅ به‌روزرسانی خودکار آمار (هر ۵ ثانیه)
- ✅ خروجی CSV از گزارش‌ها

---

## نیازمندی‌ها

برای اجرای سیستم نیاز به موارد زیر دارید:
- Node.js نسخه ۱۴ یا بالاتر
- مرورگر وب مدرن (Chrome، Firefox، Safari، Edge)

---

## نصب و راه‌اندازی

### ۱. نصب وابستگی‌ها
```bash
npm install
```

### ۲. اجرای سرور
```bash
npm start
```

### ۳. دسترسی به سیستم
پس از اجرا، سیستم در آدرس زیر در دسترس خواهد بود:
```
http://localhost:3300
```

---

## ورود به سیستم

### آدرس صفحه ورود
```
http://localhost:3300/teacher
```

### اطلاعات ورود پیش‌فرض
| فیلد | مقدار |
|------|-------|
| نام کاربری | `admin` |
| رمز عبور | `admin123` |

> ⚠️ **توجه:** پس از اولین ورود، حتماً رمز عبور خود را تغییر دهید.

### مراحل ورود:
1. به آدرس `/teacher` بروید
2. نام کاربری و رمز عبور را وارد کنید
3. روی دکمه **«ورود»** کلیک کنید

---

## داشبورد اصلی

پس از ورود موفق، داشبورد اصلی نمایش داده می‌شود که شامل بخش‌های زیر است:

### نمای کلی داشبورد
در بالای صفحه، یک نمای کلی از وضعیت سیستم نمایش داده می‌شود:

| آیتم | توضیحات |
|------|---------|
| جلسات فعال | تعداد جلسات در حال برگزاری |
| حضور امروز | درصد حضور در جلسات فعال |
| دانشجویان در انتظار | تعداد دانشجویانی که هنوز حضور ثبت نکرده‌اند |
| کل ماژول‌ها | تعداد دروس تعریف شده |

### نمودارها
- **نمودار حضور امروز:** نمودار دایره‌ای نشان‌دهنده نسبت حاضرین به غایبین
- **روند هفتگی:** نمودار خطی نشان‌دهنده تعداد حاضرین در ۷ روز گذشته

### لیست جلسات فعال
لیست تمام جلسات در حال برگزاری با امکان:
- مشاهده جزئیات حضور
- توقف موقت جلسه

### فیلتر ماژول
با استفاده از منوی کشویی می‌توانید آمار را بر اساس یک ماژول خاص فیلتر کنید.

> 🔄 **به‌روزرسانی خودکار:** تمام آمار و نمودارها هر ۵ ثانیه به‌صورت خودکار به‌روز می‌شوند.

---

## مدیریت ماژول‌ها (دروس)

ماژول‌ها همان دروس یا واحدهای درسی هستند. هر ماژول می‌تواند دانشجویان و جلسات مختص به خود را داشته باشد.

### ایجاد ماژول جدید

1. در تب **«ماژول‌ها»** روی دکمه **«افزودن ماژول»** کلیک کنید
2. اطلاعات زیر را وارد کنید:

| فیلد | توضیحات | مثال |
|------|---------|------|
| نام ماژول | نام درس (الزامی) | ریاضی ۱ |
| کد ماژول | کد درس (اختیاری) | MATH101 |
| ترم | ترم تحصیلی (اختیاری) | پاییز ۱۴۰۳ |
| تعداد کلاس‌ها | تعداد جلسات کل | ۱۵ |

3. روی **«افزودن ماژول»** کلیک کنید

### مشاهده جزئیات ماژول

روی کارت هر ماژول کلیک کنید تا جزئیات آن نمایش داده شود. در این صفحه می‌توانید:
- لیست دانشجویان را مشاهده و مدیریت کنید
- جلسات را ایجاد و مدیریت کنید

### حذف ماژول

1. روی ماژول مورد نظر کلیک کنید
2. در پایین پنجره، روی **«حذف ماژول»** کلیک کنید
3. تأیید کنید

> ⚠️ **هشدار:** با حذف ماژول، تمام دانشجویان و جلسات مربوط به آن نیز حذف خواهند شد.

---

## مدیریت دانشجویان

### افزودن دانشجو به صورت تکی

1. روی ماژول مورد نظر کلیک کنید
2. در تب **«دانشجویان»** روی **«افزودن دانشجو»** کلیک کنید
3. اطلاعات زیر را وارد کنید:

| فیلد | توضیحات |
|------|---------|
| نام (انگلیسی) | نام دانشجو به انگلیسی (الزامی) |
| نام (فارسی) | نام دانشجو به فارسی (اختیاری) |

4. روی **«افزودن دانشجو»** کلیک کنید

### افزودن گروهی دانشجویان

برای افزودن تعداد زیادی دانشجو به صورت همزمان:

1. روی ماژول مورد نظر کلیک کنید
2. در تب **«دانشجویان»** روی **«ورود گروهی»** کلیک کنید
3. لیست دانشجویان را وارد کنید (هر دانشجو در یک خط)

**فرمت ورودی:**
```
نام انگلیسی | نام فارسی | شماره دانشجویی
```

**مثال:**
```
Ali Rezaei | علی رضایی | 123456789
Sara Ahmadi | سارا احمدی | 987654321
Mohammad Hosseini | محمد حسینی
John Doe
```

> 💡 نام فارسی و شماره دانشجویی اختیاری هستند.

4. روی **«ورود دانشجویان»** کلیک کنید

### حذف دانشجو

در لیست دانشجویان، روی آیکون سطل زباله (🗑️) کلیک کنید و تأیید کنید.

---

## مدیریت جلسات

### ایجاد جلسه جدید

1. روی ماژول مورد نظر کلیک کنید
2. در تب **«جلسات»** روی **«ایجاد جلسه»** کلیک کنید
3. اطلاعات زیر را وارد کنید:

| فیلد | توضیحات | مثال |
|------|---------|------|
| شماره هفته | شماره هفته تحصیلی (اختیاری) | ۵ |
| نام جلسه | عنوان جلسه (الزامی) | جلسه پنجم - مشتق |
| کلید عبور | رمز ورود دانشجویان (الزامی) | MATH5 |

4. یا روی **«تولید تصادفی»** کلیک کنید تا کلید عبور به صورت خودکار تولید شود
5. روی **«ایجاد جلسه»** کلیک کنید

### فعال/غیرفعال کردن جلسه

- **فعال:** دانشجویان می‌توانند حضور ثبت کنند
- **غیرفعال (توقف):** ثبت حضور متوقف می‌شود

برای تغییر وضعیت:
- در لیست جلسات، روی دکمه **«توقف»** یا **«فعال»** کلیک کنید

### مشاهده حضور و غیاب جلسه

1. در **تاریخچه جلسات** یا **جلسات فعال**، روی آیکون چشم (👁️) کلیک کنید
2. لیست کامل دانشجویان با وضعیت حضور نمایش داده می‌شود

### خروجی CSV

در صفحه جزئیات جلسه، روی **«خروجی CSV»** کلیک کنید تا فایل اکسل دانلود شود.

---

## ثبت حضور توسط دانشجو

### آدرس صفحه دانشجو
```
http://localhost:3300/student
```

### مراحل ثبت حضور:

1. **دریافت کلید عبور:** استاد کلید عبور جلسه را اعلام می‌کند
2. **ورود کلید عبور:** دانشجو کلید عبور را در صفحه وارد می‌کند
3. **تأیید کلید:** روی **«تأیید کلید عبور»** کلیک می‌کند
4. **انتخاب نام:** از لیست دانشجویان، نام خود را انتخاب می‌کند
5. **ثبت حضور:** روی **«ثبت حضور»** کلیک می‌کند

> ✅ پس از ثبت موفق، پیام تأیید نمایش داده می‌شود.

### محدودیت‌ها:
- هر دانشجو فقط یک بار می‌تواند در هر جلسه حضور ثبت کند
- اگر جلسه غیرفعال باشد، ثبت حضور امکان‌پذیر نیست
- سیستم IP دانشجو را برای جلوگیری از تقلب ذخیره می‌کند

---

## گزارش‌ها و تحلیل‌ها

### دسترسی به گزارش‌ها
در داشبورد، روی تب **«گزارش‌ها»** کلیک کنید.

### کارت‌های خلاصه
- **کل دانشجویان:** تعداد کل دانشجویان ثبت‌شده
- **میانگین حضور:** درصد میانگین حضور در تمام جلسات
- **کل جلسات:** تعداد جلسات برگزار شده
- **کل ماژول‌ها:** تعداد دروس تعریف شده

### نمودارها

| نمودار | توضیحات |
|--------|---------|
| روند حضور | نمودار خطی نشان‌دهنده تغییرات حضور در جلسات |
| نمای کلی حضور | نمودار دایره‌ای نسبت حاضر به غایب |
| عملکرد ماژول‌ها | نمودار میله‌ای مقایسه درصد حضور در دروس مختلف |

### جدول گزارش دانشجویان

لیست تمام دانشجویان با اطلاعات:
- نام دانشجو
- نام ماژول
- تعداد جلسات حاضر
- کل جلسات
- درصد حضور
- وضعیت (عالی/خوب/نیاز به بهبود/بحرانی)

### فیلتر گزارش‌ها

از منوی کشویی **«همه ماژول‌ها»** می‌توانید گزارش را برای یک درس خاص فیلتر کنید.

### خروجی گزارش

- **خروجی گزارش:** دانلود گزارش کلی به صورت CSV
- **خروجی CSV:** دانلود لیست دانشجویان با آمار حضور

---

## تغییر رمز عبور

1. در هدر صفحه، روی دکمه **«رمز عبور»** کلیک کنید
2. اطلاعات زیر را وارد کنید:

| فیلد | توضیحات |
|------|---------|
| رمز عبور فعلی | رمز عبور کنونی |
| رمز عبور جدید | رمز جدید (حداقل ۶ کاراکتر) |
| تکرار رمز عبور جدید | تکرار رمز جدید |

3. روی **«بروزرسانی رمز عبور»** کلیک کنید

---

## تغییر زبان

سیستم از دو زبان پشتیبانی می‌کند:
- **EN:** انگلیسی
- **فا:** فارسی

برای تغییر زبان:
1. در هدر صفحه، دکمه‌های زبان را پیدا کنید
2. روی **«EN»** برای انگلیسی یا **«فا»** برای فارسی کلیک کنید

> 💡 انتخاب زبان ذخیره می‌شود و در بازدیدهای بعدی حفظ می‌ماند.

---

## پرسش‌های متداول

### چرا جلسه من در لیست جلسات فعال نیست؟
جلسه احتمالاً غیرفعال (متوقف) شده است. از تب جلسات در ماژول مربوطه، آن را فعال کنید.

### چرا دانشجو نمی‌تواند حضور ثبت کند؟
- مطمئن شوید جلسه فعال است
- کلید عبور صحیح وارد شده باشد
- دانشجو قبلاً حضور ثبت نکرده باشد

### چگونه می‌توانم گزارش را پرینت کنم؟
فایل CSV را دانلود کنید و در اکسل باز کنید، سپس پرینت بگیرید.

### آیا امکان ویرایش حضور و غیاب وجود دارد؟
خیر، برای حفظ صحت داده‌ها، امکان ویرایش دستی حضور وجود ندارد.

---

## پشتیبانی

در صورت بروز مشکل یا داشتن سؤال:
- مستندات را در فایل README.md مطالعه کنید
- مشکلات را در GitHub گزارش دهید

---

**نسخه:** 1.0.0
**تاریخ به‌روزرسانی:** دی ۱۴۰۳
